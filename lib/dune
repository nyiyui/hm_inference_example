(library
 (name small_functional_language)
 (modules ast eval lexer parser type_check compiler))

(menhir
 (modules parser))

(ocamllex lexer)

(test
  (name test)
  (modules test)
  (libraries small_functional_language))
